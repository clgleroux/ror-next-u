%p
  %b Brand:
  = beer.brand
%p
  %b Name:
  = beer.name
%p
  %b Cl:
  = beer.cl
%p
  %b Description:
  = beer.description
%p
  %b Notes:
  = beer.notes.length > 0 ? beer.notes.pluck(:value).sum / beer.notes.length : 'None'
